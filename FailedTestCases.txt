
test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert undefined error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert undefined error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert error without stack to ApiError correctly', () => {
  const error = new Error('Error without stack');
  error.statusCode = httpStatus.NOT_FOUND;
  delete error.stack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.NOT_FOUND);
  expect(convertedError.message).toBe('Error without stack');
  expect(convertedError.stack).toBeUndefined();
});



test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert undefined error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert error without stack to ApiError correctly', () => {
  const error = new Error('Error without stack');
  error.statusCode = httpStatus.NOT_FOUND;
  delete error.stack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.NOT_FOUND);
  expect(convertedError.message).toBe('Error without stack');
  expect(convertedError.stack).toBeUndefined();
});



describe('Error handler', () => {
  test('should handle null error input in errorHandler', () => {
    const error = null;
    const req = httpMocks.createRequest();
    const res = httpMocks.createResponse();
    const sendSpy = jest.spyOn(res, 'send');

    errorHandler(error, req, res);

    expect(res.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
    expect(sendSpy).toHaveBeenCalledWith({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    });
    expect(res.locals.errorMessage).toBeUndefined();
  });
});



test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert undefined error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert error without stack to ApiError correctly', () => {
  const error = new Error('Error without stack');
  error.statusCode = httpStatus.NOT_FOUND;
  delete error.stack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.NOT_FOUND);
  expect(convertedError.message).toBe('Error without stack');
  expect(convertedError.stack).toBeUndefined();
});



describe('Error handler', () => {
  test('should handle null error input in errorHandler', () => {
    const error = null;
    const req = httpMocks.createRequest();
    const res = httpMocks.createResponse();
    const sendSpy = jest.spyOn(res, 'send');

    errorHandler(error, req, res);

    expect(res.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
    expect(sendSpy).toHaveBeenCalledWith({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    });
    expect(res.locals.errorMessage).toBeUndefined();
  });
});



describe('Error handler', () => {
  test('should handle undefined error input in errorHandler', () => {
    const error = undefined;
    const req = httpMocks.createRequest();
    const res = httpMocks.createResponse();
    const sendSpy = jest.spyOn(res, 'send');

    errorHandler(error, req, res);

    expect(res.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
    expect(sendSpy).toHaveBeenCalledWith({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    });
    expect(res.locals.errorMessage).toBeUndefined();
  });
});



test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert undefined error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert error without stack to ApiError correctly', () => {
  const error = new Error('Error without stack');
  error.statusCode = httpStatus.NOT_FOUND;
  delete error.stack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.NOT_FOUND);
  expect(convertedError.message).toBe('Error without stack');
  expect(convertedError.stack).toBeUndefined();
});



describe('Error handler', () => {
  test('should handle null error input in errorHandler', () => {
    const error = null;
    const req = httpMocks.createRequest();
    const res = httpMocks.createResponse();
    const sendSpy = jest.spyOn(res, 'send');

    errorHandler(error, req, res);

    expect(res.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
    expect(sendSpy).toHaveBeenCalledWith({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    });
    expect(res.locals.errorMessage).toBeUndefined();
  });
});



describe('Error handler', () => {
  test('should handle undefined error input in errorHandler', () => {
    const error = undefined;
    const req = httpMocks.createRequest();
    const res = httpMocks.createResponse();
    const sendSpy = jest.spyOn(res, 'send');

    errorHandler(error, req, res);

    expect(res.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
    expect(sendSpy).toHaveBeenCalledWith({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    });
    expect(res.locals.errorMessage).toBeUndefined();
  });
});



test('should handle error with non-standard status code in errorConverter', () => {
  const error = new Error('Non-standard status code');
  error.statusCode = 999;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(999);
  expect(convertedError.message).toBe('Non-standard status code');
  expect(convertedError.isOperational).toBe(false);
});



test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert undefined error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert error without stack to ApiError correctly', () => {
  const error = new Error('Error without stack');
  error.statusCode = httpStatus.NOT_FOUND;
  delete error.stack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.NOT_FOUND);
  expect(convertedError.message).toBe('Error without stack');
  expect(convertedError.stack).toBeUndefined();
});



describe('Error handler', () => {
  test('should handle null error input in errorHandler', () => {
    const error = null;
    const req = httpMocks.createRequest();
    const res = httpMocks.createResponse();
    const sendSpy = jest.spyOn(res, 'send');

    errorHandler(error, req, res);

    expect(res.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
    expect(sendSpy).toHaveBeenCalledWith({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    });
    expect(res.locals.errorMessage).toBeUndefined();
  });
});



describe('Error handler', () => {
  test('should handle undefined error input in errorHandler', () => {
    const error = undefined;
    const req = httpMocks.createRequest();
    const res = httpMocks.createResponse();
    const sendSpy = jest.spyOn(res, 'send');

    errorHandler(error, req, res);

    expect(res.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
    expect(sendSpy).toHaveBeenCalledWith({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    });
    expect(res.locals.errorMessage).toBeUndefined();
  });
});



test('should handle error with non-standard status code in errorConverter', () => {
  const error = new Error('Non-standard status code');
  error.statusCode = 999;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(999);
  expect(convertedError.message).toBe('Non-standard status code');
  expect(convertedError.isOperational).toBe(false);
});



test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert undefined error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert error without stack to ApiError correctly', () => {
  const error = new Error('Error without stack');
  error.statusCode = httpStatus.NOT_FOUND;
  delete error.stack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.NOT_FOUND);
  expect(convertedError.message).toBe('Error without stack');
  expect(convertedError.stack).toBeUndefined();
});



describe('Error handler', () => {
  test('should handle null error input in errorHandler', () => {
    const error = null;
    const req = httpMocks.createRequest();
    const res = httpMocks.createResponse();
    const sendSpy = jest.spyOn(res, 'send');

    errorHandler(error, req, res);

    expect(res.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
    expect(sendSpy).toHaveBeenCalledWith({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    });
    expect(res.locals.errorMessage).toBeUndefined();
  });
});



describe('Error handler', () => {
  test('should handle undefined error input in errorHandler', () => {
    const error = undefined;
    const req = httpMocks.createRequest();
    const res = httpMocks.createResponse();
    const sendSpy = jest.spyOn(res, 'send');

    errorHandler(error, req, res);

    expect(res.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
    expect(sendSpy).toHaveBeenCalledWith({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    });
    expect(res.locals.errorMessage).toBeUndefined();
  });
});



test('should handle error with non-standard status code in errorConverter', () => {
  const error = new Error('Non-standard status code');
  error.statusCode = 999;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(999);
  expect(convertedError.message).toBe('Non-standard status code');
  expect(convertedError.isOperational).toBe(false);
});



test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert undefined error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert undefined error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert error without stack to ApiError correctly', () => {
  const error = new Error('Error without stack');
  error.statusCode = httpStatus.NOT_FOUND;
  delete error.stack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.NOT_FOUND);
  expect(convertedError.message).toBe('Error without stack');
  expect(convertedError.stack).toBeUndefined();
});



describe('Error handler', () => {
  test('should handle null error input in errorHandler', () => {
    const error = null;
    const req = httpMocks.createRequest();
    const res = httpMocks.createResponse();
    const sendSpy = jest.spyOn(res, 'send');

    errorHandler(error, req, res);

    expect(res.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
    expect(sendSpy).toHaveBeenCalledWith({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    });
    expect(res.locals.errorMessage).toBeUndefined();
  });
});



describe('Error handler', () => {
  test('should handle undefined error input in errorHandler', () => {
    const error = undefined;
    const req = httpMocks.createRequest();
    const res = httpMocks.createResponse();
    const sendSpy = jest.spyOn(res, 'send');

    errorHandler(error, req, res);

    expect(res.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
    expect(sendSpy).toHaveBeenCalledWith({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    });
    expect(res.locals.errorMessage).toBeUndefined();
  });
});



test('should handle error with non-standard status code in errorConverter', () => {
  const error = new Error('Non-standard status code');
  error.statusCode = 999;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(999);
  expect(convertedError.message).toBe('Non-standard status code');
  expect(convertedError.isOperational).toBe(false);
});



test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert undefined error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert non-error object to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = "Simple error string";
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert undefined error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert error without stack to ApiError correctly', () => {
  const error = new Error('Error without stack');
  error.statusCode = httpStatus.NOT_FOUND;
  delete error.stack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.NOT_FOUND);
  expect(convertedError.message).toBe('Error without stack');
  expect(convertedError.stack).toBeUndefined();
});



describe('Error handler', () => {
  test('should handle null error input in errorHandler', () => {
    const error = null;
    const req = httpMocks.createRequest();
    const res = httpMocks.createResponse();
    const sendSpy = jest.spyOn(res, 'send');

    errorHandler(error, req, res);

    expect(res.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
    expect(sendSpy).toHaveBeenCalledWith({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    });
    expect(res.locals.errorMessage).toBeUndefined();
  });
});



describe('Error handler', () => {
  test('should handle undefined error input in errorHandler', () => {
    const error = undefined;
    const req = httpMocks.createRequest();
    const res = httpMocks.createResponse();
    const sendSpy = jest.spyOn(res, 'send');

    errorHandler(error, req, res);

    expect(res.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
    expect(sendSpy).toHaveBeenCalledWith({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    });
    expect(res.locals.errorMessage).toBeUndefined();
  });
});



test('should handle error with non-standard status code in errorConverter', () => {
  const error = new Error('Non-standard status code');
  error.statusCode = 999;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(999);
  expect(convertedError.message).toBe('Non-standard status code');
  expect(convertedError.isOperational).toBe(false);
});



test('should convert null error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert undefined error to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should convert non-error object to ApiError with INTERNAL_SERVER_ERROR status', () => {
  const error = "Simple error string";
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.stack).toBeUndefined();
});



test('should handle extremely long error messages and stack traces without crashing', () => {
  const longMessage = 'a'.repeat(10000);
  const longStack = 'b'.repeat(10000);
  const error = new ApiError(httpStatus.BAD_REQUEST, longMessage, false);
  error.stack = longStack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const sendSpy = jest.spyOn(res, 'send');

  errorHandler(error, req, res);

  expect(sendSpy).toHaveBeenCalledWith(
    expect.objectContaining({
      code: error.statusCode,
      message: error.message,
      stack: error.stack,
    })
  );
  expect(res.locals.errorMessage).toBe(error.message);
});



test('should convert null error to ApiError with statusCode 500 in errorConverter', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should convert null error to ApiError with statusCode 500 in errorConverter', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should convert undefined error to ApiError with statusCode 500 in errorConverter', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should convert null error to ApiError with statusCode 500 in errorConverter', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should convert undefined error to ApiError with statusCode 500 in errorConverter', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should handle ApiError without statusCode in errorHandler by setting statusCode to 500', () => {
  const error = new ApiError(undefined, 'Undefined Status');
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const sendSpy = jest.spyOn(res, 'send');

  // Set environment to production
  config.env = 'production';

  errorHandler(error, req, res);

  expect(sendSpy).toHaveBeenCalledWith(
    expect.objectContaining({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    })
  );
  expect(res.locals.errorMessage).toBe(error.message);
  expect(res._getData()).not.toHaveProperty('stack');
});



test('should convert null error to ApiError with statusCode 500 in errorConverter', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should convert undefined error to ApiError with statusCode 500 in errorConverter', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should handle ApiError without statusCode in errorHandler by setting statusCode to 500', () => {
  const error = new ApiError(undefined, 'Undefined Status');
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const sendSpy = jest.spyOn(res, 'send');

  // Set environment to production
  config.env = 'production';

  errorHandler(error, req, res);

  expect(sendSpy).toHaveBeenCalledWith(
    expect.objectContaining({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    })
  );
  expect(res.locals.errorMessage).toBe(error.message);
  expect(res._getData()).not.toHaveProperty('stack');
});



test('should handle errors with exceptionally long stack traces in errorConverter and errorHandler', () => {
  const longStack = Array(1001).fill('at function').join('\n');
  const error = new Error('Long stack trace error');
  error.stack = longStack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(error.message);
  expect(convertedError.stack).toBe(longStack);

  // Verify response in development environment
  config.env = 'development';
  errorHandler(convertedError, req, res);

  const responseData = res._getJSONData();
  expect(responseData).toHaveProperty('stack');
  expect(responseData.stack).toBe(longStack);

  // Reset environment
  config.env = process.env.NODE_ENV;
});



test('should convert null error to ApiError with statusCode 500 in errorConverter', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should convert undefined error to ApiError with statusCode 500 in errorConverter', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should handle ApiError without statusCode in errorHandler by setting statusCode to 500', () => {
  const error = new ApiError(undefined, 'Undefined Status');
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const sendSpy = jest.spyOn(res, 'send');

  // Set environment to production
  config.env = 'production';

  errorHandler(error, req, res);

  expect(sendSpy).toHaveBeenCalledWith(
    expect.objectContaining({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    })
  );
  expect(res.locals.errorMessage).toBe(error.message);
  expect(res._getData()).not.toHaveProperty('stack');
});



test('should handle errors with exceptionally long stack traces in errorConverter and errorHandler', () => {
  const longStack = Array(1001).fill('at function').join('\n');
  const error = new Error('Long stack trace error');
  error.stack = longStack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(error.message);
  expect(convertedError.stack).toBe(longStack);

  // Verify response in development environment
  config.env = 'development';
  errorHandler(convertedError, req, res);

  const responseData = res._getJSONData();
  expect(responseData).toHaveProperty('stack');
  expect(responseData.stack).toBe(longStack);

  // Reset environment
  config.env = process.env.NODE_ENV;
});



test('should handle errors with undefined or null properties in errorConverter and errorHandler', () => {
  const error = new Error();
  error.statusCode = null;
  error.message = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);

  errorHandler(convertedError, req, res);

  const responseData = res._getJSONData();
  expect(responseData).toHaveProperty('code', httpStatus.INTERNAL_SERVER_ERROR);
  expect(responseData).toHaveProperty('message', httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
});



test('should convert null error to ApiError with statusCode 500 in errorConverter', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should convert undefined error to ApiError with statusCode 500 in errorConverter', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should handle ApiError without statusCode in errorHandler by setting statusCode to 500', () => {
  const error = new ApiError(undefined, 'Undefined Status');
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const sendSpy = jest.spyOn(res, 'send');

  // Set environment to production
  config.env = 'production';

  errorHandler(error, req, res);

  expect(sendSpy).toHaveBeenCalledWith(
    expect.objectContaining({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    })
  );
  expect(res.locals.errorMessage).toBe(error.message);
  expect(res._getData()).not.toHaveProperty('stack');
});



test('should handle errors with exceptionally long stack traces in errorConverter and errorHandler', () => {
  const longStack = Array(1001).fill('at function').join('\n');
  const error = new Error('Long stack trace error');
  error.stack = longStack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(error.message);
  expect(convertedError.stack).toBe(longStack);

  // Verify response in development environment
  config.env = 'development';
  errorHandler(convertedError, req, res);

  const responseData = res._getJSONData();
  expect(responseData).toHaveProperty('stack');
  expect(responseData.stack).toBe(longStack);

  // Reset environment
  config.env = process.env.NODE_ENV;
});



test('should handle errors with undefined or null properties in errorConverter and errorHandler', () => {
  const error = new Error();
  error.statusCode = null;
  error.message = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);

  errorHandler(convertedError, req, res);

  const responseData = res._getJSONData();
  expect(responseData).toHaveProperty('code', httpStatus.INTERNAL_SERVER_ERROR);
  expect(responseData).toHaveProperty('message', httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
});



test('should convert string error to ApiError with statusCode 500 in errorConverter', () => {
  const error = "A simple error string";
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(error);
  expect(convertedError.isOperational).toBe(false);
});



test('should convert null error to ApiError with statusCode 500 in errorConverter', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should convert undefined error to ApiError with statusCode 500 in errorConverter', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should handle ApiError without statusCode in errorHandler by setting statusCode to 500', () => {
  const error = new ApiError(undefined, 'Undefined Status');
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const sendSpy = jest.spyOn(res, 'send');

  // Set environment to production
  config.env = 'production';

  errorHandler(error, req, res);

  expect(sendSpy).toHaveBeenCalledWith(
    expect.objectContaining({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    })
  );
  expect(res.locals.errorMessage).toBe(error.message);
  expect(res._getData()).not.toHaveProperty('stack');
});



test('should handle errors with exceptionally long stack traces in errorConverter and errorHandler', () => {
  const longStack = Array(1001).fill('at function').join('\n');
  const error = new Error('Long stack trace error');
  error.stack = longStack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(error.message);
  expect(convertedError.stack).toBe(longStack);

  // Verify response in development environment
  config.env = 'development';
  errorHandler(convertedError, req, res);

  const responseData = res._getJSONData();
  expect(responseData).toHaveProperty('stack');
  expect(responseData.stack).toBe(longStack);

  // Reset environment
  config.env = process.env.NODE_ENV;
});



test('should handle errors with undefined or null properties in errorConverter and errorHandler', () => {
  const error = new Error();
  error.statusCode = null;
  error.message = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);

  errorHandler(convertedError, req, res);

  const responseData = res._getJSONData();
  expect(responseData).toHaveProperty('code', httpStatus.INTERNAL_SERVER_ERROR);
  expect(responseData).toHaveProperty('message', httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
});



test('should convert string error to ApiError with statusCode 500 in errorConverter', () => {
  const error = "A simple error string";
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(error);
  expect(convertedError.isOperational).toBe(false);
});



test('should sanitize error messages containing injection attack patterns in errorHandler', () => {
  const xssMessage = "<script>alert('XSS')</script>";
  const error = new ApiError(httpStatus.BAD_REQUEST, xssMessage);
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const sendSpy = jest.spyOn(res, 'send');

  errorHandler(error, req, res);

  expect(sendSpy).toHaveBeenCalledWith(
    expect.objectContaining({
      code: error.statusCode,
      message: expect.not.stringContaining('<script>'),
    })
  );
  expect(res.locals.errorMessage).toBe(xssMessage);
});



test('should convert null error to ApiError with statusCode 500 in errorConverter', () => {
  const error = null;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should convert undefined error to ApiError with statusCode 500 in errorConverter', () => {
  const error = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  expect(() => {
    errorConverter(error, req, res, next);
  }).not.toThrow();

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);
});



test('should handle ApiError without statusCode in errorHandler by setting statusCode to 500', () => {
  const error = new ApiError(undefined, 'Undefined Status');
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const sendSpy = jest.spyOn(res, 'send');

  // Set environment to production
  config.env = 'production';

  errorHandler(error, req, res);

  expect(sendSpy).toHaveBeenCalledWith(
    expect.objectContaining({
      code: httpStatus.INTERNAL_SERVER_ERROR,
      message: httpStatus[httpStatus.INTERNAL_SERVER_ERROR],
    })
  );
  expect(res.locals.errorMessage).toBe(error.message);
  expect(res._getData()).not.toHaveProperty('stack');
});



test('should handle errors with exceptionally long stack traces in errorConverter and errorHandler', () => {
  const longStack = Array(1001).fill('at function').join('\n');
  const error = new Error('Long stack trace error');
  error.stack = longStack;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(error.message);
  expect(convertedError.stack).toBe(longStack);

  // Verify response in development environment
  config.env = 'development';
  errorHandler(convertedError, req, res);

  const responseData = res._getJSONData();
  expect(responseData).toHaveProperty('stack');
  expect(responseData.stack).toBe(longStack);

  // Reset environment
  config.env = process.env.NODE_ENV;
});



test('should handle errors with undefined or null properties in errorConverter and errorHandler', () => {
  const error = new Error();
  error.statusCode = null;
  error.message = undefined;
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
  expect(convertedError.isOperational).toBe(false);

  errorHandler(convertedError, req, res);

  const responseData = res._getJSONData();
  expect(responseData).toHaveProperty('code', httpStatus.INTERNAL_SERVER_ERROR);
  expect(responseData).toHaveProperty('message', httpStatus[httpStatus.INTERNAL_SERVER_ERROR]);
});



test('should convert string error to ApiError with statusCode 500 in errorConverter', () => {
  const error = "A simple error string";
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const next = jest.fn();

  errorConverter(error, req, res, next);

  expect(next).toHaveBeenCalledWith(expect.any(ApiError));
  const convertedError = next.mock.calls[0][0];
  expect(convertedError.statusCode).toBe(httpStatus.INTERNAL_SERVER_ERROR);
  expect(convertedError.message).toBe(error);
  expect(convertedError.isOperational).toBe(false);
});



test('should sanitize error messages containing injection attack patterns in errorHandler', () => {
  const xssMessage = "<script>alert('XSS')</script>";
  const error = new ApiError(httpStatus.BAD_REQUEST, xssMessage);
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const sendSpy = jest.spyOn(res, 'send');

  errorHandler(error, req, res);

  expect(sendSpy).toHaveBeenCalledWith(
    expect.objectContaining({
      code: error.statusCode,
      message: expect.not.stringContaining('<script>'),
    })
  );
  expect(res.locals.errorMessage).toBe(xssMessage);
});



test('should handle operational ApiError correctly in development environment in errorHandler', () => {
  const error = new ApiError(httpStatus.NOT_FOUND, 'Resource not found', true);
  const req = httpMocks.createRequest();
  const res = httpMocks.createResponse();
  const sendSpy = jest.spyOn(res, 'send');
  const loggerSpy = jest.spyOn(logger, 'error').mockImplementation(() => {});

  config.env = 'development';

  errorHandler(error, req, res);

  expect(sendSpy).toHaveBeenCalledWith(
    expect.objectContaining({
      code: error.statusCode,
      message: error.message,
      stack: error.stack,
    })
  );
  expect(res.locals.errorMessage).toBe(error.message);
  expect(loggerSpy).not.toHaveBeenCalled();

  // Reset environment and mocks
  config.env = process.env.NODE_ENV;
  loggerSpy.mockRestore();
});


